
const USER_TYPE = {
  USER: 'USER'
};

const STATUS = {
  ACTIVE  : 'ACTIVE',
  INACTIVE: 'INACTIVE',
  DELETED : 'DELETED',
};

const DEFAULT_PAGE_NUMBER = 1;
const DEFAULT_PAGE_SIZE   = 10;

const VOTE_TYPE = {
  UPVOTE  : 'UPVOTE',
  DOWNVOTE: 'DOWNVOTE',
}

export default {
  VOTE_TYPE,
  USER_TYPE,
  STATUS,
  DEFAULT_PAGE_NUMBER,
  DEFAULT_PAGE_SIZE,
}